[build]
  command = "npm run build"
  publish = "dist"

# SPA routing: serve index.html for every path so client-side routes like
# /dashboard/messages and /services work on refresh and deep links.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
